#!/bin/sh

MONO=
BEE="$PWD/../PackageCache/com.unity.tiny@0.15.3-preview/DotsPlayer/bee~/bee.exe"
BEE=$(printf %q "$BEE")
if [ "$APPDATA" == "" ] ; then
    MONO=mono
fi
cmdToRun="${MONO} ${BEE} $*"
if [ $# -eq 0 ]; then
    eval "${cmdToRun} -t"
  else
    eval "${cmdToRun}"
fi
